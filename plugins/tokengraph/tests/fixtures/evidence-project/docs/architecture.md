# Fixture architecture

The route calls a tenant-aware patient service. The service records an audit event. PostgreSQL row-level security is mandatory. Release packaging ships one bundled runtime entry point and install metadata. Benchmark tasks must preserve constraints, recommend focused tests, and disclose when raw reads remain necessary. This deliberately verbose fixture prose contributes a realistic raw-context baseline while remaining deterministic and local.
